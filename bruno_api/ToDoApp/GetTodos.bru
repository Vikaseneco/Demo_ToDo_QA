meta {
  name: GetTodos
  type: http
  seq: 7
}

get {
  url: http://localhost:4000/api/tasks
  body: none
  auth: inherit
}

tests {
  expect response.status == 200
  expect Array.isArray(response.body)
}

settings {
  encodeUrl: true
}
