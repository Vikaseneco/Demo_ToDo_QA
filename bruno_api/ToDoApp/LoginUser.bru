meta {
  name: LoginUser
  type: http
  seq: 5
}

post {
  url: http://localhost:4000/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username": "tstsapmsp123",
    "password": "tstsapmsp123"
  }
}

tests {
  test("Status code is 400", function () {
    expect(res.getStatus()).to.equal(400);
  });
}

settings {
  encodeUrl: true
}
